<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme.OPorqueEuTeAmo é o tema principal do aplicativo -->
    <style name="Theme.OPorqueEuTeAmo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Cores primárias -->
        <item name="colorPrimary">@color/primary_pink</item>
        <item name="colorPrimaryVariant">@color/primary_pink_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Cores secundárias -->
        <item name="colorSecondary">@color/decoration_pink</item>
        <item name="colorSecondaryVariant">@color/decoration_rose</item>
        <item name="colorOnSecondary">@color/white</item>
        
        <!-- Cores de fundo -->
        <item name="android:colorBackground">@color/background_start</item>
        <item name="colorSurface">@color/background_start</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorOnSurface">@color/text_primary</item>
        
        <!-- Barra de status -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary_pink_light</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>

